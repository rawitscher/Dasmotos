{
  "name": "elsewhere-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "npm --prefix trip-itineraries run setup",
    "start": "npm --prefix trip-itineraries start",
    "dev": "npm --prefix trip-itineraries run dev"
  }
}
